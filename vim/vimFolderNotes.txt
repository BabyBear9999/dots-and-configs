mkdir .vim 
cd .vim 
mkdir autoload ftplugin plugged plugin

#inside autoload
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

#inside ftplugin
#put make.vim & tex.vim inside

#inside plugged 
#vimtex

#inside plugin
#put vimtex.vim inside
